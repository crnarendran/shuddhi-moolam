---
type: handover
date: 2026-08-13
from: Claude Code (Opus)
to: Antigravity
---
# Session handover — 2026-08-13

Everything below is **on `dev` and deployed** (dev deploys green). Nothing is
promoted to staging/prod yet. All three branches were content-aligned before
this session's `dev`-only work.

## Shipped to dev this session (pending promotion)
- **SM-40** — price unit normalization at the dashboard **load boundary**: the
  10 `Rs/tonne` commodities show as ₹/kg; storage/Sheet still ₹/tonne.
  (`toCanonicalPriceRecord` in reporting.ts, applied in App.tsx.)
- **SM-41** — read-only company sharing (a/b/c): `invitations` collection +
  `viewerUids` on companies; callables `createInvitation` / `acceptInvitation`
  / `resendInvitation` / `revokeInvitation`; **Resend** email
  (RESEND_API_KEY secret, sender `sai-shuddhi-moolam-no-reply@narensportal.com`,
  domain verified; secret binding LIVE after the deploy SA got Secret Manager
  access). Owner Share panel; accept-on-load (`?invite=token`); read-only
  viewer view; **context switcher**.
- **SM-42** — premium entitlement gate: `entitlements/{uid}` (user-read,
  backend-write); creating own companies/materials requires premium; **admin
  panel** (`Settings ▸ Admin`) lists users + toggles plans (`setUserPlan`,
  `listUserPlans`). **Settings ▸ Commodities is premium-only.** **Monitor is
  admin-only.**
- **Role split (just landed):** admin (Monitor + Admin panel + pipeline_runs +
  the plan callables) = **crnarendran only**; founders (crnarendran +
  mvsaikishore) = grandfathered **premium**. See `isAdminEmail` vs
  `isFounderEmail` in firestore.rules, ADMIN/FOUNDER lists in
  functions/src/sharing/entitlements.ts and dashboard/src/hooks/usePlan.ts.
- **Free-user UX:** free users have no "My workspace" — they auto-land in the
  company shared with them; the switcher hides "My workspace" for them.

## Remaining work (please continue)
1. **Guard: an admin can't downgrade a founder / themselves to free.** Founders
   are grandfathered premium so the toggle is a confusing no-op. In
   `setUserPlan` (functions/src/sharing/entitlements.ts) reject setting a
   founder email to `free`; in `AdminPage.tsx` disable/hide the toggle for
   founder rows. (FOUNDER_EMAILS already exists in both.)
2. **SM-43 — store prices in kg everywhere** (Firestore + master Sheet), not
   just dashboard display. Full plan + the **critical double-division gotcha**
   in `planning/backlog/SM-43-tonne-to-kg-storage-migration.md`. Key point:
   removing SM-40's `toCanonicalPriceRecord` MUST ship in the same release as
   the storage migration, and Gemini keeps extracting native tonne (normalize
   after extraction).
3. **Promote the whole dev batch → staging → production** (SM-40, SM-41,
   SM-42, the role split, + anything you add). Use the Release Manager role and
   the hardened rules (SM-41-era retro): source-parity drift check, post-merge
   verify of `deploy.yml`/`config.ts`/`firestore.rules`, back-merge after.
   Branches are currently `main`/`staging` ancestors of `dev` (clean).
4. **After prod:** archive shipped tickets (SM-40/41/42) to the archive per §7,
   and release the sharing/plans backlog rows.

## Gotchas / context
- **Single Firestore project** (`sai-shuddhi-moolam`); `firestore` rules deploy
  on EVERY branch push (last-writer-wins). companies/user_settings/entitlements/
  invitations are **unsuffixed** (shared across envs); pipeline_runs/
  historical_prices are **env-suffixed**.
- **`config.ts`**: production runs with `APP_ENV='main'` (from the branch name),
  so anything env-conditional must treat `'main'` as prod (`isProduction`).
- **New functions must be added to the `FUNCS` list in `.github/workflows/
  deploy.yml`** or they won't deploy.
- **Secret-bound functions** need the deploy SA to have Secret Manager access
  (already granted for RESEND_API_KEY).
- Deploy runs `npm run lint` (functions eslint: 80-char, `@returns`) + `npm
  test` as gates.

## Test checklist (dev)
- Free user (varrshinisai): no "My workspace"; lands in shared Company A;
  Companies tab = upsell; no Commodities/Monitor/Admin; reports scoped to the
  company's commodities; can't create a company (rules + UI).
- Owner: Share → invite → email arrives; status Waiting→Accepted; resend/
  revoke work.
- Admin (crnarendran): sees Monitor + Admin panel + user list + can toggle
  plans. mvsaikishore: premium but NO Monitor/Admin.
