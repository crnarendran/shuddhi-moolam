rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pipeline_runs/{document=**} {
      allow read: if request.auth != null && request.auth.token.email == 'crnarendran@gmaill.com';
      allow write: if false;
    }
  }
}
